# Fritzing 部件开发规则（fritzing-parts-langhua）

本仓库是 Fritzing 自定义部件库。开发、修改、重构任何部件时，**必须先遵循以下规则**。
详细踩坑记录见 `docs/part-dev-guide.md`（本文件是其「规则版」摘要 + 记忆中的硬性要求）。

## 0. 防过拟合工作规则（2026-09-06 用户定）

用户指出我存在"过拟合"：已经做对了却不停手，为修小问题叠加修补，最后制造出多余色块/残留。
做任何图形/元件修改时**必须遵守**：

1. **Done 即停**：用户已认可的结果，除非用户明确要求，绝不再"顺手优化/兜底/加固"。
2. **一次一改**：一次只改一个变量；每次改动必须等用户确认，不连续叠加修补。
3. **过拟合信号 = 停手信号**：若修一个小问题需要叠加第 2 个以上补偿性改动
   （额外的补丁 rect、补线、mask、裁剪等），说明方向错了 →
   立即停止、回退到最近一个"用户认可/干净"的状态、向用户重新对齐，而不是继续打补丁。
4. **以用户观察为准**：用户以 Inkscape 打开的实样为准；cairosvg 渲染可能与
   Inkscape 不一致，不要凭自己的渲染结果反复"自嗨式"微调。
5. **保留干净基线**：每个被用户认可的版本是回退点；怀疑某处时先问"哪个是基线"，
   不擅自叠加。

反例教训（SMA 干净 icon，2026-09-06）：把两只脚从金底几何裁掉后已干净；又为
"自己渲染里看到的细缝"加"柱体兜底矩形 + x=4.0 补线" → 制造出多余色块，用户要求撤回。

## 1. 设计偏好：工业风，拒绝浮夸

- 图标/图形**直接用元件真实外观**（PCB 焊盘、原理图符号原样），不放大形状、不自作主张加装饰。
- 不加黑点/阴影/高光等装饰；尺寸自然、留边距；忠实还原元件本来样子 = 工业风。
- 参考 Fritzing 官方元件 icon 风格（干净、写实、无装饰）。
- 反例教训：把焊盘 icon 放大成粗环加黑点 → 用户强烈不满。禁止这种"浮夸风"。

## 2. 芯片类元件固定工作流（新建芯片元件必走）

1. 先根据芯片文档（**最好英文 datasheet**）画芯片的 **icon svg**
2. 把 icon svg 应用到**面包板 svg**
3. 根据芯片文档画**原理图 svg**
4. 根据芯片文档画 **PCB svg**

**芯片引脚序号逆时针排列**：引脚号从 pin1 起按**逆时针**顺序排布（与 IC 标准编号一致）。
例：SOP16 转接板排针 = 下排 1→8（左→右）、上排 16→9（右→左），路径 1(左下)→8(右下)→9(右上)→16(左上)。

**原理图引脚编号**：标注在引脚线**上方**（不被导线穿过），字号略小于芯片名/符号名。

**晶振原理图 = 直接复用 Fritzing 官方 `svg/core/schematic/crystal.svg` 几何**（viewBox **15.1×10.8**，
晶片 x=4.67/10.43、方框 6.47..8.63 × 1.08..9.72、导线 y=5.4、引脚号 x≈1.54/12.4 y=4.35、字号 2.5），
与官方晶振元件**同尺寸同比例**（曾画 60×44 仍被用户嫌大，2026-09-01 改为 1:1 复用官方）。

## 3. 开发板/模块类部件模型（面包板 = 开发板、原理图 = 芯片）

- 参考 `svg/ESP32-S3-WROOM-1`：面包板视图画**整块开发板**（排针 + USB/按键/晶振/FPC 等板级特征），
  原理图视图画**芯片符号**，两者通过连接器按**信号名**映射（非 1:1 引脚数）。
- 板级额外电源脚（5V 轨、多余 GND）用**面包板专用连接器**（无原理图/PCB 视图）。
- GND/3V3/5V 等内部互通用 `<buses>` 声明。
- 排针丝印标签 = 板子实际丝印；连接器名 = 芯片脚名（两边信号一致，连线不乱）。
- 开发板面包板图中，插针标签统一放在**内侧**：顶排标签在引脚下方、底排标签在引脚上方（都朝向板心）。
- **标签不能与引脚重叠**：丝印文字必须避开连接器圆点/焊盘，保持可读。

## 3b. 非插针式元件面包板 = 绿色转接板（新规则）

- **非插针式元件**（SMD、BGA 等不能直插面包板的）做面包板视图时，**首选**画在**绿色 PCB 转接板**上：
  - 绿色板（如 `#00aa44`、**直角**）+ 按**管脚数**安排足够的 2.54mm 排针（每个连接器对应一个插针）。
  - 板上元件**直接复用该元件自己的 icon**（`svg.icon.<id>_icon.svg` 的 `<g id="icon">`），**1:1 真实尺寸**、
    **竖放居中**；不加 3D/高光/阴影装饰。
  - **排针必须落在面包板孔距网格上**：各排针间距/行距必须是 **2.54mm 的整数倍**（如 2.54、7.62=3×2.54、10.16=4×2.54），
    否则插不进面包板孔。
  - **转接板排针按 2mm 直径焊盘 + 0.97mm 直径针孔** 设计（圆形焊盘环 + 中央暗孔）。
  - **排针不得与元件重叠**：2mm 焊盘各向伸出半径 1mm，排针行距必须 ≥ 元件高度 + 2mm（两侧各 1mm），
    否则焊盘会压到元件（如 3.5mm 高晶振需行距 ≥7.62mm）；必要时加大行距（2.54 整数倍）与板高。
  - **转接板管脚数字标注规则**（2026-09-04 用户定，ETA3425S2F 起；2026-09-17 补「内侧」）：
    管脚数字**逆时针旋转 90°**（`transform="rotate(-90 x y)"`）、**居中与焊盘中心对齐**、
    用 Fritzing 支持的字体（如 `DroidSans`），数字与焊盘**空隙 0.3mm**（`≈11.8` 内部单位）。
    ★ **数字必须落在焊盘的「内侧」（朝板心那一侧）** —— 用户 2026-09-17：
    「已经是每次必错的地方了」。板端排针最容易错（顺手就画到板边外侧去了）；
    **内侧放不下时**（元件占满板中央）要**把排针往板边移、给数字腾出位置**，
    **不许**改到外侧。例：DSIC01LS-P 排针 y 由 200/800 移到 100/900（板高不变）、
    RJ45-8P8C 排针由 250 移到 100。
  - **开关类元件的拨杆位置**（2026-09-17 用户定，SK-12D02VG3 / DSIC01LS-P 起）：
    拨杆在滑动方向上**不要居中** —— 要让**拨杆的一个边缘与本体中线**（多位开关 =
    该位的中间线）**对齐**，因为拨杆是停在实际档位上的、不会停在两档中间。
    例：SK-12D02VG3 拨杆宽 2、左缘对齐本体中线（即行程 `2 TRAVEL` 的一端）；
    DSIC01LS-P 拨杆（= 立创滑槽/拨柄框宽 1.27 = 本体宽 2.54 的一半）左缘同样
    对齐本体中线。
    ★ 拨杆与本体相接的那一侧是**直角**（拨杆是插进本体的），只有**外端**才倒圆
    或开 V 型缺口（与杆帽契合）。
  - **转接板面积尽量最小**：在满足「排针网格对齐 + 元件能放下」的前提下，转接板尺寸越小越好，
    以降低 PCB 生产成本。
  - 例：SMD 晶振 3225/3215 的画法（见 `svg/Crystal-3225`、`svg/Crystal-3215`）。
- **次选**：定制开发板（面包板 = 整块开发板，见第 3 节）。

## 4. 仓库目录与打包约定

- 源文件（`part.<id>.fzp` + 4 个 `svg.<view>.<id>_<view>.svg` + 生成脚本 `gen_part.py`）
  放在 `svg/<部件名>/`。
- 打包好的 `.fzpz` 输出到**仓库顶层 `fzpz/`**（脚本里 `OUT_DIR/../../fzpz`）。
- `.fzpz`（zip）**内部平铺**：`part.<id>.fzp` + `svg.breadboard.<id>_breadboard.svg` 等，不放子目录。
- 但 `.fzp` 的 `image=` 引用**必须用子目录路径**：`icon/`、`breadboard/`、`schematic/`、`pcb/`。
- **外部素材归置规则**（2026-09-05 用户定）：仓库内各部件可**跨部件引用其它部件目录的 svg**
  （如 `../TXW8301`、`../TypeC16Pin`、`../WS2812B/5050`、`../CH340E` 的 icon/breadboard，作为 1:1 图形素材），
  但**不得**引用仓库外的文件（`D:\Downloads\…`、Fritzing 安装目录 `C:\…\Fritzing\…` 等）；
  确需复用的**外部 svg/fzpz 素材**统一收进 **`svg/_assets/`**（仓库内、随部件一起提交），
  供多个脚本相对引用（例：`ws2812b_core_breadboard.svg`、`jst_xh4a_breadboard.svg`）。
  **外部素材必须随附其版权/许可文件**（2026-09-05 用户定）：在 `svg/_assets/` 放对应
  `LICENSE-<asset>.txt`，注明来源仓库/作者/许可（例：ws2812b ← Fritzing core 官方 CC-BY-SA 3.0
  `LICENSE-ws2812b_core_breadboard.txt`；JST XH4A ← orionrobots/fritzing-parts MIT
  `LICENSE-jst_xh4a_breadboard.txt`），避免无授权传播第三方图形。
- **小而单点的外部图形可内联进 gen_part.py 常量**（例：SparkFun SMD-1101NE 按钮 2.8KB，
  2026-09-05 内联为 `_BTN_1101NE_INNER` 后删除原独立 svg），避免留 stray 文件。
- **手工对齐式面包板图（2026-09-15 起，CH347T/CH347F 用）**：面包板版式不再全靠脚本摆 ——
  用户在 Inkscape 里拿**实物照片当底图**把排针/元件对到实际位置，存成
  `svg/<部件>/svg.breadboard.<部件>_breadboard_byHand.svg`（草稿，`*_byHand.svg` 已 gitignore），
  再由 `tools/byhand_export.py svg\<部件>` 导出 **`svg/<部件>/byHand_tables.py`（入库、纯数据）**，
  `gen_part.py` 读这张表出图。**导出器只做"逐字照搬"**（不平移/不对齐/不修用户的图）：
  单位跟文档的 width/height 走、`style=` 优先于同名属性、`SHAPES` 严格按文档次序（保叠放次序）、
  字号规则见 `tools/README.md`；发现手工版里圆环与焊盘偏心这类问题**改手工版**（留 `.bak`），
  不要改导出器去替用户对齐。
- **★ 手工改过的板：一律以「板上丝印」为准**（2026-09-16 用户定，CH347T 起）：
  - **丝印文字**（手工版 `<text>` / 板级原理图 / 实物照片）**才是名字的权威** ——
    人眼能看见、能在 Inkscape 里校对和修改；**藏在 svg 源码里的名字**
    （pad 元素上的 `connectorname`，常常是 Ctrl+D 复制来的）**人看不见、不作为依据**。
  - 所以：网名/脚名有疑问 → 先看丝印（Inkscape 里那几个字），再看板级原理图 PDF；
    `gen_part.py` 里改名的表（`_PAD_RENAME` / `_JUMPERS` …）**要标出处**，
    且**照丝印的原文写**（丝印写 `RXD` 就写 `RXD`，写 `3V3` 就写 `3V3`，别"顺手补全"成芯片脚名）。
  - 反例教训（CH347T，2026-09-16）：手工版里 pad 的 `connectorname` 是从芯片脚列表复制来的
    （`RTS0/GPIO1/MISO/TDO`、`VCC`…），而**同一批焊盘旁边的丝印写的是** `RTS`/`3V3`/`MISO` ——
    照源码名字建总线，就把板上的"线"和芯片脚搅在一起了；改成照丝印才与实物对得上。
  - 同一根线在两处丝印写法不同时（CH347T 的 `RXD`(P7) ↔ `RXD0`(跳线)），
    用 `_LINE_TIES` **显式**并成一条总线（名字对不上就没法自动并）。

## 5. 生成脚本硬性要求（踩过的坑，写 gen_part.py 时必须遵守）

- **gen_part.py 不得依赖任何仓库外文件**（2026-09-05 用户定）：运行时 `open`/`ZipFile`
  只允许访问 `OUT_DIR` 本目录、仓库内其它部件目录、或 `svg/_assets/`；
  严禁硬编码本机绝对路径（`D:\Downloads\…`、`C:\…\Fritzing\…` 等）作读取源——
  换机器/删文件即报错或静默退化（例：WS2812B 5050/2020 曾硬编码 Fritzing 安装目录 core svg，
  缺文件时 `try/except` 吞掉错误、静默产出简化版面包板/icon，与已提交 svg 不一致）。
  已内联/入库的资产：`svg/_assets/ws2812b_core_breadboard.svg`（Fritzing core）、
  `svg/_assets/jst_xh4a_breadboard.svg`、TX-AH 的按钮 `_BTN_1101NE_INNER` 常量。
  注：注释/文档字符串里写数据来源路径（如 `D:\Downloads\…pdf`）不算依赖，可保留。
- SVG 几何常量用**整数**（`body_w/body_h/bx/by` 传 int，如 `25,25,50,50` 而非 `25.0`），
  否则 `{bx}` 输出 `25.0` 与已有源文件产生**字节级 diff**。算术如 `bx+body_w/2` 仍得浮点。
- 每个视图 `<svg>` 头部**必须同时有 `width` 和 `height`**；breadboard/pcb 视图用 **mm 单位**
  （如 `width="9mm" height="9mm" viewBox="7 7 18 18"`）。
  只写 width 缺 height → Fritzing「不能为 svg 创建渲染」，导入报错。
- **schematic 视图的物理尺寸由 `width/height` 属性决定**（带单位）：若只写无单位数字，
  Fritzing 按默认 DPI 解释 → 尺寸会超大（2026-09-01 FPC 原理图踩坑）。要精确控制 mm 尺寸：
  用 **in 单位 + 1000 单位=1in** 比例（如 FPC05-2H10PX 参考 `width="0.20448959in" height="1.17305in"`
  = 5.194×24.715mm，viewBox 204.49×973.05）。
- **面包板视图若需插进面包板孔**（转接板/排针部件）：内部坐标用 **100 单位 = 2.54mm** 约定
  （`1mm = 39.37` 单位），排针/连接器中心必须落在 **100 单位的整数倍**上（如 100、200、300），
  `width/height(mm)` 用 `单位/100×2.54` 换算，否则 Fritzing 对不上面包板孔（3215/3225 曾因此插不进孔）。
- **别加非标准 `gorn` 属性**。
- **丝印文字：Inkscape 读数 ≠ SVG 文字属性（2026-09-06 用户定，SMA 面包板 +/GND 踩坑）**：
  - Inkscape 状态栏对 `<text>` 的 X/Y 读数不是元素 x/y 属性：X≈文字**左缘**、Y≈文字**顶边**（基线−字高 cap）。
  - `text-anchor="middle"` 会让 X 读数≠x 属性（x 变成中心）→ 需要读数精确对齐时**去掉 `text-anchor`**
    （默认左对齐，X 读数 = x 属性，可精确对上）。
  - Y 读数 = 基线 − 字高；字高随字体/字形/字号变（DroidSans：`+`≈0.56em、`GND`≈0.72em，不一致）→
    **不能写死公式**，每字号/字形实测一次；同字号下调基线时读数平移量 = 基线平移量（线性修正）。
  - 通用做法：丝印统一 DroidSans、去掉 anchor、x 给左缘；y 先给基线再按一次 Inkscape 实测校准；
    或干脆以 cairosvg 渲染的视觉位置为准，不抠 Inkscape 读数。
- **原理图引脚端点 / terminal 画法（2026-09-06 用户定，参照 BAT54S / SparkFun）**：
  - `connectorNterminal` 用**极小不可见 rect**（`x/y = 引脚线末端`，`width/height="0.0001"`，
    `stroke="none" fill="none"`）——靠引脚灰线末端吸附连线；**禁止画大黑圆/夸张端点**
    （SMA-PJ1.7 反例：加 r≈1.8 实心黑点被用户要求撤回）。
  - 引脚线 `connectorNpin`：`stroke="#787878"`、`stroke-width="0.75"`、round cap、`class="pin"`；
    引脚编号文字灰 `#8C8C8C`、DroidSans、字号 2.5、放引线上方（不写名称文字时按用户要求标号）。
- **元件内多焊盘同网络 = 每 pad 独立 connector + `<buses>` 互联（2026-09-06 用户定，SMA 4×GND 踩坑）**：
  - PCB 上多个物理 pad 若同网络（如同地外壳的多个 GND 焊盘），**每个 pad 必须是一个独立
    connector（各自都能接线）**，再用 `<buses>` 把同网络的 connector 互联
    （Fritzing：点任一 pad 时同 net 的其它 pad 一起高亮）。
  - **绝不可**把多个 pad 合并成一个 connector——那会只剩一个可接线节点（用户视为"添乱"）。
  - 同一网络总线可命名 `id`（如 `<bus id="GND">`），便于识别。
  - 现实语义：元件一体地壳的多地脚 = "各自可接线 + 物理同地"，两者都要在 `.fzp` 里表达。
- **裸露焊盘（EPAD / 底板 / 中央散热盘）必须独立成网**（2026-09-15 用户定）：
  - **不进 `<buses>` 的 GND 总线** —— 它要**布线时特意接到 GND**，自动合并会让人以为已经接好了；
  - 名字**照各自数据手册**用 `EP` / `EPAD`（不叫 `GND`；叫 GND 又不在 GND 总线里 = 看着连上其实没连），
    description 写清 `exposed thermal pad, route to GND`；
  - 也不要把 EPAD 的脚号当作"板级地脚"发给板上的 GND 焊盘（CH347F 曾把 0# 发给了排针的 GND 焊盘 → 已纠正）。
  - 已就位：RT6150 的 `EP`、TX-AH 的 `EPAD1/2`、TXW8301 的 `EPAD`；
    2026-09-15 纠正：CH347F 的 0#（原写 GND 并进 GND 总线 → 改名 `EPAD` + 移出总线 + 不再占板级地脚号）、
    ESP32-S3-WROOM-1 的中央焊盘（原 `GND` 在 GND 总线里 → 改名 `EPAD` + 移出）。
  - 机器守：`tools/fzp_check.py` 第 ⑥ 条（名字匹配 `EPAD`/`EP` 的 connector 不许出现在任何 `<bus>` 里）。
- **板上网络：名字 = 板上丝印，且「只有名字完全一致才共 bus」**（2026-09-16 用户定，CH347T 起）：
  - 排针/焊盘的 connector 名用**板子实际丝印**（`CS0/MISO/SCK/MOSI`、`TMS/TCK/TDO/TDI/TRST`、
    `SCL/SDA`、`RXD0/TXD0/RTS0/GP1…`），**不是**芯片脚名；芯片脚名只在"该焊盘**直连**芯片脚"时才用。
  - 总线**只按名字完全相同**归并，**不做别名/近似合并**（`MISO` 绝不能因为芯片 7 脚也有 MISO
    功能就并上去）→ 名字写对，网络自然就对；`fzp_check.py` 第 ⑦ 条守住"同名必同总线"。
  - **焊接跳线（3 焊点一排）接的是三条不同的线**（CH347T 的 P9–P14 = "IO SWITCH" 配置区）：
    上脚 = UART0 侧那条线、**中脚 = 芯片脚本身**、下脚 = SPI/I2C 侧那条线 ——
    所以**中脚才是芯片脚**（面包板上代表该芯片脚的就是它），上/下脚各自与板上的**同名线**共 bus。
    ⚠ 反例教训：曾把三脚当成"同一个芯片脚的三种叫法"而全并到芯片脚（错），
    又曾改成"完全不成网"（也错）—— 正确是**每个脚各接自己那条线**。
  - 跳线**盖不住**的脚是**直连**（CH347T 的 9 脚 ↔ `TRST/CS1/DTR0`、15 脚 ↔ `DCD0/GP4`）：
    名字对不上，必须在 `BUSES` 里**显式**并到该芯片脚。
- 连接器是灵魂：各视图的 `connectorNpin` id、`connectorname` 必须与 `.fzp` 一致，否则无法连线。
- **矩形封装（方框）原理图符号规则**（2026-09-01 用户定，参照 CH32V203C8T6）：
  - **排布按封装排数选**（2026-09-15 定，仓库先例一致）：**两排封装（SOP/TSSOP/MSOP）用左右两列**
    —— 左列 = pin1..N/2（上→下）、右列 = 上→下 N..N/2+1（即从下往上数），与手册顶视图一致且逆时针
    （CH340C 16 脚 / CH340E 10 脚 / CH347T TSSOP20）；**四排封装（QFN/LQFP）用四边**
    （CH347F QFN28 / CH32V203C8T6 LQFP48）。列数定了其余规则照下面走。
  - 左右引脚数字在**引线上方**（不与引线相交，从左至右）；上下引脚数字在**引脚左侧**（`rotate(270)`，从下至上）。
  - 引脚名**都在元件框内**，书写方向与引脚数字相同。
  - **数字与引脚名整图同字号**（如 FN=14）。
  - **名 / 数字 / 引脚线颜色一致**（普通脚黑、按数据手册/用户指定标蓝 `#0000c8`；例：CH32V203C8T6 按数据手册第 51 页仅 1/2/3/4 脚（VBAT/PC13/PC14/PC15）蓝，其余全黑，非"ADC 自动标蓝"）。
  - （长）方框原理图：**引脚名中线与引脚对齐**（左右名**手动基线偏移** y=引脚y+round(FN×0.35)；
    **不能用 `dominant-baseline`**（cairosvg/Fritzing 不支持→左右名不居中）；上下名 `text-anchor="middle"`）。
  - 引脚名与边框保持**一个字符间距**（CH=FN），分别居左 / 右 / 下 / 上
    （左 x=BX0+CH、右 x=BX1−CH；**注意**：上下名 `text-anchor="middle"`+`rotate(270)` 时文字以锚点为中心，
    故上 y=BY0+CH+文字高/2、下 y=BY1−CH−文字高/2，才能让顶/底边距边框一个字符）。
  - **数字与引脚名互不重叠**：间距/字号/边距要保证四角上下名不与相邻左右名交叉（必要时加大间距与符号尺寸）。
  - **四角无引脚区（2026-09-01 用户定，解决"紧凑 vs 不重叠"矛盾）**：四个角留空白，宽度 = 最长引脚名宽 + 1 字符宽
    （`CORNER=(max_len+1)×int(FN×0.58)`）；四边引脚从 CORNER 后开始排（左/下 y,x = 边+ CORNER+P/2+iP），
    框 = `12P + 2×CORNER`（**完全对称**）。上下名与左右名靠四角空白隔开、互不交叉 →
    **引脚间距 P 可任意小**（只需 > 名字高度 FN，如 P=100=2.54mm），无需加大间距/加高框/牺牲对称。
    反例教训：曾误以为"引脚间距须 ≥ 名字宽"而把 P 拉到 6.1mm、符号 78mm，且"加高框避四角"导致左右引脚错位。
  - **上下数字/名对齐**：上下引脚数字**右侧距引脚半个字符高**（FN/2，数字锚点 x−FN）；
    上下引脚名**左侧与数字右侧对齐**（名居中于引脚 x，左缘 = 数字右缘 = x−FN/2）。

## 6. 工具链（blender2step 工作流）

- Fritzing 电路 → **Gerber(RS-274X) + PNP** → PCB 工厂 / 贴片。
- Fritzing → FritzingToBlender → Blender（外壳）→ blender2step 导出 STEP → FreeCAD 验证 → 模具工厂。
- 配套：Inkscape（元件 SVG）、OpenCASCADE（STEP 引擎）。
- 交付硬件时需产出 **Gerber + PNP**。
- 本库元件风格与 `fritzing-parts-langhua` 一致。

## 7. 协作约定

- 改动元件库/公共文件前**先与用户对齐**再动手。
- 项目级特例：AuroraTessellation-NFC 项目只做/改原理图，不画面包板；但**新建芯片元件**按第 2 节流程做全部视图。
